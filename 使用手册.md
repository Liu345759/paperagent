# paperagent 使用手册

paperagent 在你自己的电脑上运行。网页负责填写和预览，本机 Python 程序负责检索文献、读文件、写综述。它不是一个新的大模型，也不经过 Cursor。

公开仓库：https://github.com/Liu345759/paperagent

## 1. 需要什么

- Windows 电脑
- Python 3.13（命令 `py -3.13` 能用即可）
- 浏览器
- 能访问互联网（检索 arXiv、Semantic Scholar）

选「本地规则」时不需要任何 API Key。  
选「调用大模型」时，需要你自己的 DeepSeek / 通义千问 / OpenAI（或兼容接口）的 Key。

## 2. 启动

在仓库文件夹里打开终端，运行：

```text
py -3.13 paperagent_bridge.py
```

看到类似下面两行后，用浏览器打开第一个地址：

```text
paperagent: http://127.0.0.1:8766/paperagent_v59.html
api:        http://127.0.0.1:8766/api/run
```

关掉这个终端，页面就无法检索和写稿。不要只打开 html 文件，要通过上面的地址访问。

服务绑在本机 `127.0.0.1`，手机或别人的电脑不能直接打开你这一份。别人要自己下载仓库、在自己电脑上启动。

## 3. 首页

### 研究问题

一句话写清对象和你要回答的事。这一栏会真正决定检索和正文写什么。

### 补充说明

年份范围、只要哪些对象、不要哪些内容，写在这里会起作用。例如「2021到2026」「只要火星和木卫」。不要把年份当成检索词乱填到研究问题里，年份请写在补充说明。

### 生成方式

- **本地规则**：本机程序按规则检索、拼中文综述、出图、排参考文献。不调用外部大模型。速度较快，文风更像按文献拼起来的稿子。
- **调用大模型**：检索、读 PDF、画图、排参考文献仍由本机程序做；六个步骤的文字改由你填写的模型来写。

接口、模型、API 地址、API Key 只在选「调用大模型」时出现。Key 只存在你的浏览器和本机程序之间，不要发给别人。

### 个人材料库

首页只显示数量。点「打开」进入材料库页。

- **加入**：把 PDF、表格、笔记入库。PDF 会抽正文；纯截图、扫描件通常抽不出字。
- **勾选**：勾上的才会带进这一次研究。
- **筛选**：文件多时按文件名或类型过滤。
- **删除**：从库里去掉，不只是取消勾选。

材料库存在你自己的浏览器里，换电脑或换浏览器不会自动带过去。

## 4. 六个步骤

点「开始研究」后，右侧是论文预览，左侧按步骤推进。

1. 文献检索与筛选  
2. 文献解析  
3. 信息抽取与综述  
4. 实验数据初步统计  
5. 图表生成与插入  
6. 参考文献格式化（GB/T 7714）

可以一键出结果，也可以逐步确认。某一步点「重跑」只重做那一步，不会默认改其他步。

确认时看闭环检验：文献要能溯源（DOI 或 arXiv 编号），正文引用编号要和参考文献对得上，年份要符合补充说明。未通过时先改问题、补充说明或重跑检索，不要强行当终稿。

## 5. 对话和下载

对话里可以补要求（例如只要某类对象、写长一点）。短指令会并进下一次生成。

下载：

- **下载 HTML**：浏览器里打开的完整论文
- **下载 Word**：便于再改

历史在本机 `paper_history` 文件夹。可以退回某次结果，六个步骤和当时的文献列表会一起恢复。

## 6. 别人怎么用

把仓库链接发给对方即可：

https://github.com/Liu345759/paperagent

对方按第 2 节在自己电脑上启动。各用各的材料库和 Key。不要把你的 API Key 放进仓库或聊天记录。

## 7. 常见情况

| 情况 | 说明 |
| --- | --- |
| 页面打不开 | 先确认 `paperagent_bridge.py` 还在运行，并用 `127.0.0.1:8766` 打开 |
| 检索失败 | 检查网络；补充说明里的年份不要写成检索词 |
| 调用大模型失败 | 检查 Key、模型名、API 地址是否填对 |
| PDF 没抽出字 | 多半是扫描件或图片，请换可选中文字的 PDF |
| 手机打不开 | 正常。当前只在启动它的那台电脑的浏览器里用 |

## 8. 文件说明

- `paperagent_v59.html`：页面
- `paperagent_bridge.py`：本机接口
- `使用手册.md`：本说明
- `paper_history/`：运行后在本机生成，不必提交到 GitHub
